# CAAsXploreer JSON Export Pipeline — Python dependencies
# Install with: pip install -r scripts/pipeline/requirements.txt
#
# Python >= 3.10 required.

# Core data processing
pandas>=2.0.0
openpyxl>=3.1.0          # .xlsx reading engine for pandas
xlrd>=2.0.1              # .xls reading engine for pandas (legacy files)

# Configuration
PyYAML>=6.0.1            # config.yaml loading (used in main.py Step 3+)

# Data validation (used in normalize.py Step 3+)
# pandera>=0.18.0         # uncomment when normalize stage is implemented

# Utilities
tqdm>=4.66.0             # progress bars for large workbooks (optional)
