# AI Execution Note: Homepage Analytics

- Current implementation target is homepage analytics validation, not UI redesign.
- Current branch already contains:
  - Homepage refactor
  - Provisional real-data adapter
- Future AI tasks must preserve:
  - `app/src/main.tsx` routes
  - Ranking/state pages unless explicitly asked
  - `app/dist` exclusion from commits
- Future AI tasks should treat:
  - Current homepage as implementation baseline
  - JSON analytics files as provisional source candidates pending validation
